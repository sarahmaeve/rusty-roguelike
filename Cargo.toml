[package]
name = "impossible-mission-two"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
bevy_light_2d = "0.5"
rand = "0.8"

# Speed up debug builds significantly
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
